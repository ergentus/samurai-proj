import React from 'react'
import s from './Settings.module.css'

type SettingsTypeProps = {}

const Settings = (props: SettingsTypeProps) => {
   return (
      <div>Settings</div>
   )
}

export default Settings